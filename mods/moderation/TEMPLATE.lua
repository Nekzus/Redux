local _config = {
	name = "",
	desc = "",
	usage = "",
	aliases = {},
	cooldown = 0,
	level = 0,
	direct = false,
}

local _function = function(data)

end

return {config = _config, func = _function}
